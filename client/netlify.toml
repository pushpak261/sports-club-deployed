[build]
  base = "client"
  command = "CI=false npm run build"
  publish = "build"

# SPA routing — forward all paths to index.html so React Router works on page refresh
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
